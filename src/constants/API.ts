
export const LOCAL_API_HOSTNAME = "http://127.0.0.1:8000"

// export const PROD_API_HOSTNAME = "https://..."

// uncomment when you have a production API
// export const API_HOSTNAME = process.env.NODE_ENV === "production" ? "https://..." : LOCAL_API_HOSTNAME

export const API_HOSTNAME = LOCAL_API_HOSTNAME;